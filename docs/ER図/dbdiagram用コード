Table users {
  id integer [primary key]
  last_name string [not null]
  first_name string [not null]
  last_name_kana string [not null]
  first_name_kana string [not null]
  nickname string [not null]
  email string [not null, unique]
  password string [not null]
  birth_date date [not null]
  created_at datetime [not null]
  updated_at datetime [not null]
}

Table items {
  id integer [primary key]
  name string [not null]
  description text [not null]
  condition_id integer [not null, note: 'Active Hash']
  price integer [not null]
  shipping_fee_status integer [not null]
  shipping_day_id integer [not null, note: 'Active Hash']
  sender_prefecture_id integer [not null, note: 'Active Hash']
  category_id integer [not null, note: 'Active Hash']
  created_at datetime [not null]
  updated_at datetime [not null]
  user_id integer [not null, ref: > users.id]
}

Table orders {
  id integer [primary key]
  created_at datetime [not null]
  updated_at datetime [not null]
  user_id integer [not null, ref: > users.id]
  item_id integer [not null, ref: - items.id]
}

Table delivery_addresses {
  id integer [primary key]
  postal_code string [not null]
  prefecture_id integer [not null, note: 'Active Hash']
  city string [not null]
  address string [not null]
  building_name string
  phone_number string [not null]
  created_at datetime [not null]
  updated_at datetime [not null]
  order_id integer [not null, ref: - orders.id]
}

Table comments {
  id integer [primary key]
  content text [not null]
  created_at datetime [not null]
  updated_at datetime [not null]
  user_id integer [not null, ref: > users.id]
  item_id integer [not null, ref: > items.id]
}